# LinkedIn Clone Backend (Node/Express)

## Setup
- Copy `.env.example` to `.env` and fill values.
- `npm install`
- `npm run dev` (requires nodemon) or `npm start`

Cloudinary is used for image uploads. Set `CLOUDINARY_*` env vars in your environment.
